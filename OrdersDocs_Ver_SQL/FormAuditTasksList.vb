Public Class FormAuditTasksList
    Public Sub New()

        ' This call is required by the designer.
        InitializeComponent()

        ' Add any initialization after the InitializeComponent() call.

        'Начало блок за Проверка дали има избрани задачи
        'Вдигане на флаг ако е избран който и да е чекбокс
        'If FormMain.chkListBoxReason.SelectedItems.Count > 0 Then

        'End If
        'Край на блока за проверка

    End Sub



End Class